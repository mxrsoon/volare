on:
  push:
    branches:
      - 'main'
      - 'feature/deploy-workflow'

name: 'Deploy to GCE'

jobs:
  deploy:
    name: "Deploy"
    runs-on: 'ubuntu-latest'
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: 'Checkout'
        uses: 'actions/checkout@v3'

      - id: 'setup-ssh-keys'
        name: 'Setup SSH keys'
        env:
          SSH_PRIV_KEY: ${{ secrets.GCP_SSH_PRIVATE_KEY }}
          SSH_PUB_KEY: ${{ secrets.GCP_SSH_PUBLIC_KEY }}
          SSH_CONFIG: ${{ secrets.GCP_SSH_CONFIG }}
        run: |
          mkdir -p ~/.ssh
          echo $SSH_PRIV_KEY > ~/.ssh/id_gcp
          echo $SSH_PUB_KEY > ~/.ssh/id_gcp.pub
          echo $SSH_CONFIG > ~/.ssh/config

      - id: 'ssh'
        name: 'SSH command'
        run: |
          echo ~/.ssh/config
          ssh volare-server sudo ls -lA /var/volare